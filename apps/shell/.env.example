# Shell App Environment Variables
# Copy to .env.local and set values for local development
# Set these in Vercel dashboard for production

# MFE URLs (optional - defaults to Vercel URLs in production, localhost in dev)
MFE_AGENT_TOOLKIT_URL=http://localhost:3001
MFE_COMPONENT_LIBRARY_URL=http://localhost:3002
MFE_DESIGN_REVIEW_URL=http://localhost:3003

# Theme (optional)
BRAND_THEME=stackone-green
