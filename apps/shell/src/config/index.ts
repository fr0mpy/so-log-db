export {
  PROVIDER_REGISTRY,
  KNOWN_PROVIDERS,
  getProviderLogoUrl,
  isKnownProvider,
} from './providers'

export { PAGINATION, TABLE } from './pagination'

// Re-export UI library config for components using @/config alias
export {
  SPRING,
  SPRING_CONFIG,
  DURATION,
  OFFSET,
  OPACITY,
  SCROLL,
  PRESS,
  THEME_TIMING,
  LOADING,
  SPINNER,
  TOAST,
  ARIA,
  LABEL,
  PLACEHOLDER,
  SR_ONLY,
  BREAKPOINTS,
  MEDIA_QUERIES,
} from '@stackone-ui/core/config'
