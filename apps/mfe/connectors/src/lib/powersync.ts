/**
 * PowerSync initialization stub
 * TODO: Install @powersync/web and configure with backend
 */

export interface PowerSyncConfig {
  backendUrl: string
  database: string
}

export function initPowerSync(_config: PowerSyncConfig) {
  // Stub - real implementation requires:
  // 1. @powersync/web package
  // 2. PowerSync backend service
  // 3. Schema definition matching Postgres tables
  // Stub: Real implementation requires @powersync/web package and backend service
  void _config
}
