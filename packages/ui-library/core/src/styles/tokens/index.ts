/**
 * Token exports - primitive design values.
 */

import { Spacing } from './spacing'
import { Sizing } from './sizing'
import { Typography } from './typography'

export { Spacing, Sizing, Typography }

export const Tokens = {
  Spacing,
  Sizing,
  Typography,
} as const
