/**
 * Style utilities for the UI library.
 *
 * Use raw Tailwind classes directly in styles.ts files for IntelliSense support.
 * Reference values from comments:
 *   - h-11 = 44px (WCAG 2.5.8 AAA touch target)
 *   - h-5 = 20px (matches text-sm line-height for skeletons)
 */

// ============================================================================
// TYPE EXPORTS
// ============================================================================

export { type Breakpoint, type ResponsiveValue } from './responsive'
