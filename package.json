{
  "name": "stackone-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "overrides": {
      "esbuild": "0.21.5"
    }
  },
  "scripts": {
    "clean:ports": "for port in 3000 3001 3002 3003 5173; do lsof -ti :$port | xargs kill -9 2>/dev/null; done; echo 'Ports cleared'",
    "clean:next": "rm -rf apps/shell/.next apps/mfe/*/.next",
    "predev:all": "pnpm clean:ports && pnpm clean:next",
    "dev": "turbo dev --no-daemon",
    "dev:all": "echo '\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n  StackOne Dev Servers\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n  Harness:         http://localhost:5173\n  Shell:           http://localhost:3000\n  Agent Toolkit:   http://localhost:3001\n  Design Review:   http://localhost:3003\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n' && turbo dev --no-daemon",
    "dev:shell": "turbo dev --no-daemon --filter @stackone/shell",
    "dev:agent-toolkit": "turbo dev --no-daemon --filter @stackone/mfe-agent-toolkit",
    "dev:design-review": "turbo dev --no-daemon --filter @stackone/mfe-design-review",
    "dev:component-library": "turbo dev --no-daemon --filter @stackone/mfe-component-library",
    "dev:harness": "turbo dev --no-daemon --filter @stackone-ui/harness",
    "build": "turbo build",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "babel-plugin-react-compiler": "^1.0.0",
    "next": "^15.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  }
}